# server {
#     listen 80;
#     listen [::]:80;

#     server_name k-room.space www.k-room.space;
#     server_tokens off;

#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }

#     location / {
#         return 301 https://$host$request_uri
#     }

#     # location / {
#     #     proxy_pass http://k-room-client:3001/;
#     # }
# }

# server {
#     listen 443 default_server ssl http2;
#     listen [::]:443 ssl http2;

#     server_name k-room.space;

#     ssl_certificate /etc/nginx/ssl/live/k-room.space/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/live/k-room.space/privkey.pem;

#     location / {
#         proxy_pass http://k-room-client:3001/;
#     }
# }
http {
    server_tokens off;
    charset utf-8;

    # always redirect to https
    server {
        listen 80 default_server;

        server_name _;

        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl http2;
        # use the certificates
        ssl_certificate     /etc/letsencrypt/live/{domain}/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/{domain}/privkey.pem;
        server_name {domain};
        root /var/www/html;
        index index.php index.html index.htm;


        location / {
            proxy_pass http://k-room-client:3001/;
        }

        location ~ /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
    }
}