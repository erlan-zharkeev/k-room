{
  "name": "k-room",
  "version": "1.0.0",
  "repository": "git@gitlab.com:k-room/k-room.git",
  "author": "<zharkeev.post@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "zsh scripts/dev.sh",
    "husky-prepare": "husky install",
    "sync:public-config": "node scripts/sync-public-config.mjs",
    "format:fix": "prettier --write .",
    "lint:fix": "npm --prefix client run lint:fix-all && npm --prefix server run lint:fix-es",
    "e2e": "playwright test -c config/playwright/playwright.config.ts",
    "e2e:ui": "playwright test -c config/playwright/playwright.config.ts --ui",
    "check": "npm --prefix client run lint:all && npm --prefix client run build && npm --prefix server run lint:es && npm --prefix server run typecheck && npm --prefix server run build && npm run test && prettier --check .",
    "test": "npm --prefix client run test && npm --prefix server run test --if-present"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "husky": "^8.0.2",
    "prettier": "^2.8.8"
  }
}
