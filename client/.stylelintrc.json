{
  "extends": ["stylelint-config-standard-scss", "stylelint-config-rational-order", "stylelint-config-prettier-scss"],
  "rules": {
    "selector-class-pattern": null,
    "no-descending-specificity": null,
    "max-nesting-depth": 2,
    "selector-nested-pattern": [
      "^(?!.*&(?:__|--)).*$",
      {
        "message": "Do not use BEM element or modifier nesting through `&__` or `&--`. Write full class selectors explicitly."
      }
    ]
  }
}
