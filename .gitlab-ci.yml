stages:
  - build
  - publish

variables:
  SERVER_IMAGE: ketjo/k-room_server
  CLIENT_IMAGE: ketjo/k-room_client

build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker-compose build --output ./artifacts
  only:
    - main
  artifacts:
    paths:
      - server
      - client

publish:
  stage: publish
  image: docker:latest
  services:
    - docker:dind
  script:
    - images
    - docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD
    - docker load -i artifacts/server
    - docker load -i artifacts/client
    - docker tag ${SERVER_IMAGE}:latest ${SERVER_IMAGE}:${CI_COMMIT_SHA}
    - docker tag ${CLIENT_IMAGE}:latest ${CLIENT_IMAGE}:${CI_COMMIT_SHA}
    - docker push ${SERVER_IMAGE}:${CI_COMMIT_SHA}
    - docker push ${CLIENT_IMAGE}:${CI_COMMIT_SHA}
  only:
    - main
